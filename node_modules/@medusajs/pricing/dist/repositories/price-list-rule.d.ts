import { Context, CreatePriceListRuleDTO, DAL, UpdatePriceListRuleDTO } from "@medusajs/types";
import { DALUtils } from "@medusajs/utils";
import { SqlEntityManager } from "@mikro-orm/postgresql";
import { PriceListRule } from "../models";
export declare class PriceListRuleRepository extends DALUtils.MikroOrmBaseRepository {
    protected readonly manager_: SqlEntityManager;
    constructor({ manager }: {
        manager: SqlEntityManager;
    });
    find(findOptions?: DAL.FindOptions<PriceListRule>, context?: Context): Promise<PriceListRule[]>;
    findAndCount(findOptions?: DAL.FindOptions<PriceListRule>, context?: Context): Promise<[PriceListRule[], number]>;
    delete(ids: string[], context?: Context): Promise<void>;
    create(data: CreatePriceListRuleDTO[], context?: Context): Promise<PriceListRule[]>;
    update(data: UpdatePriceListRuleDTO[], context?: Context): Promise<PriceListRule[]>;
}
