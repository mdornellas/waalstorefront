import { Context, DAL, UpdatePriceListDTO } from "@medusajs/types";
import { DALUtils } from "@medusajs/utils";
import { SqlEntityManager } from "@mikro-orm/postgresql";
import { PriceList } from "../models";
import { CreatePriceListDTO } from "../types";
export declare class PriceListRepository extends DALUtils.MikroOrmBaseRepository {
    protected readonly manager_: SqlEntityManager;
    constructor({ manager }: {
        manager: SqlEntityManager;
    });
    find(findOptions?: DAL.FindOptions<PriceList>, context?: Context): Promise<PriceList[]>;
    findAndCount(findOptions?: DAL.FindOptions<PriceList>, context?: Context): Promise<[PriceList[], number]>;
    delete(ids: string[], context?: Context): Promise<void>;
    create(data: CreatePriceListDTO[], context?: Context): Promise<PriceList[]>;
    update(data: Omit<UpdatePriceListDTO, "rules">[], context?: Context): Promise<PriceList[]>;
}
