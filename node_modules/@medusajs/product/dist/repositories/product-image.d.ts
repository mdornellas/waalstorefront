import { Context, DAL } from "@medusajs/types";
import { Image } from "../models";
import { SqlEntityManager } from "@mikro-orm/postgresql";
import { DALUtils } from "@medusajs/utils";
export declare class ProductImageRepository extends DALUtils.MikroOrmAbstractBaseRepository<Image> {
    protected readonly manager_: SqlEntityManager;
    constructor({ manager }: {
        manager: SqlEntityManager;
    });
    find(findOptions?: DAL.FindOptions<Image>, context?: Context): Promise<Image[]>;
    findAndCount(findOptions?: DAL.FindOptions<Image>, context?: Context): Promise<[Image[], number]>;
    upsert(urls: string[], context?: Context): Promise<Image[]>;
    delete(ids: string[], context?: Context): Promise<void>;
    create(data: unknown[], context?: Context): Promise<Image[]>;
}
